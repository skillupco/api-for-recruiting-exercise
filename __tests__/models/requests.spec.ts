import { } from 'lodash';
import Request, { IRequest } from '../../src/models/requests';
import requestsData from '../../src/data/requests';

describe('models/requests', () => {
  const data = requestsData;
  it('should fail', () => {
    throw new Error();
  })
});
